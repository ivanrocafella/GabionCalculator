export interface ResponseCostWorkModel {
  ExchangeDollar: number // unity of measure = som / $
  TimeWeldingOneCrossBar: number // unity of measure = h
  TimeSettingEguip: number // unity of measure = h
  PNR: number // unity of measure = $
  Margin: number // unity of measure = %
  DateUpdate: string;
  Id: number;
}
